import { AsideListGroupDirective } from './aside.list.group.directive';

describe('AsideListGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new AsideListGroupDirective();
    expect(directive).toBeTruthy();
  });
});
