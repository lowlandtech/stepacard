import { SidebarCloseDirective } from './sidebar-close.directive';

describe('SidebarCloseDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarCloseDirective();
    expect(directive).toBeTruthy();
  });
});
